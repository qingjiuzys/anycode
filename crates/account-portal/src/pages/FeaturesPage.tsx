import { FeatureGalaxy } from "../components/FeatureGalaxy";

/** Full capability galaxy — each planet is a product feature. */
export function FeaturesPage() {
  return (
    <div className="nx-site nx-site--features">
      <FeatureGalaxy mode="page" />
    </div>
  );
}
