# Media generation

When the user asks to generate an image or video, call `GenerateImage` or `GenerateVideo` if those tools appear in your tool list. Do not suggest FFmpeg, MoviePy, or manual shell workflows unless the tool is unavailable or returns a configuration error.
