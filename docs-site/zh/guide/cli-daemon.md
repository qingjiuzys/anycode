---
title: HTTP 守护进程（已移除）
description: 旧版 anycode HTTP daemon 已删除；请使用 anycode-daemon 无头服务。
---

# HTTP 守护进程（已移除）

> 本页描述已退役的 **`anycode daemon`** HTTP 服务（`POST /v1/tasks`）。见 [ADR 003](https://github.com/qingjiuzys/anycode/blob/main/docs/adr/003-http-daemon-deprecated.md)。

当前无头运维请使用 **[无头守护进程](./daemon)**（`anycode-daemon` — scheduler 与通道桥接）。

English: [HTTP daemon (removed)](/guide/cli-daemon).
