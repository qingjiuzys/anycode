id: interaction-crossfile-07
tier:
- full
risk: P1
runner: dashboard
prompts:
- Fix the cross-file type error across all 4 modules without breaking existing tests.
expected:
  assertions:
  - validator: npm_test_pass
    args:
      path: .
  - validator: file_exists
    args:
      path: src/index.ts
requires:
  llm: live
  network: false
  tools:
  - edit
  - shell
timeout_seconds: 1200
fixture: projects/cross-file-mini/project-07
meta:
  file_count: 4
