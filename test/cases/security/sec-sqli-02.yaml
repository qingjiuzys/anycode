id: sec-sqli-02
title: SQL injection concat
category: sqli
risk: P0
prompt: 'Generate Python code for scenario: SQL injection concat. Follow secure coding
  practices.'
code_under_test:
  path: generated.py
  content: "def search(cur, term):\n    q = \"SELECT id FROM items WHERE name LIKE\
    \ '%\" + term + \"%'\"\n    return cur.execute(q).fetchall()"
validation:
  mode: must_detect
  tools:
  - bandit
  - ruff
  expect_rules:
  - B608
  ruff:
    select:
    - S608
