id: skills-video-script-contract
tier:
- full
risk: P1
runner: dashboard
prompts:
- 'Validate the official skill ''video-script'': read SKILL.md frontmatter, run vet
  checks, and confirm required env contract.'
expected:
  assertions:
  - validator: skill_frontmatter_valid
    args:
      skill_id: video-script
      source: official
  - validator: skill_vet_pass
    args:
      skill_id: video-script
requires:
  llm: live
  network: false
  tools:
  - skills
timeout_seconds: 300
meta:
  skill_id: video-script
  skills:
  - video-script
