id: skills-novel-writer-contract
tier:
- full
risk: P1
runner: dashboard
prompts:
- 'Validate the official skill ''novel-writer'': read SKILL.md frontmatter, run vet
  checks, and confirm required env contract.'
expected:
  assertions:
  - validator: skill_frontmatter_valid
    args:
      skill_id: novel-writer
      source: official
  - validator: skill_vet_pass
    args:
      skill_id: novel-writer
requires:
  llm: live
  network: false
  tools:
  - skills
timeout_seconds: 300
meta:
  skill_id: novel-writer
  skills:
  - novel-writer
