id: office-pptx-12
tier:
- full
risk: P2
runner: dashboard
prompts:
- 'Complete office task pptx #12 using templates in workspace; satisfy all structural
  assertions.'
expected:
  assertions:
  - validator: file_exists
    args:
      path: output/deck-12.pptx
  - validator: pptx_slide_count
    args:
      path: output/deck-12.pptx
      min: 5
  - validator: pptx_title_contains
    args:
      path: output/deck-12.pptx
      slide: 0
      text: Overview
requires:
  llm: live
  network: false
  tools:
  - office
timeout_seconds: 1200
fixture: office/templates/pptx
complexity: complex
