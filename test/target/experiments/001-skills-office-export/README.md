# 001 - Skills Office Export

验证本地 skills 能完成办公报表链路：Markdown 表格导出 CSV，并将销售日报 Markdown 生成真实 PDF。

运行：

```bash
node test/target/experiments/001-skills-office-export/run.mjs
```

产物写入 `out/`，结果写入 `out/result.json`。
