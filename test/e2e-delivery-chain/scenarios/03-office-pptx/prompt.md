# 03 — Office PPTX

生成 3 页汇报 PPT，保存为 `artifacts/june_sales_deck.pptx`：

1. 封面：六月销售汇报
2. 数据页：引用 fixtures/sales_june.csv 要点
3. 结论页
- 优先使用 `anycode-ppt`（HTML 幻灯片）；若需原生 `.pptx` 再用 `presentation-commercial-delivery`
