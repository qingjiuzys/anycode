---
description: Agent 交付标准：自测、配置、回归、产物路径
alwaysApply: true
---

# Agent 交付标准

## 完成定义
写码不等于完成。宣称 done 前必须：
1. 启动/重启相关服务，确认端口与进程
2. 端到端走通用户主路径（支付、SSE、生成、部署等按任务相关项）
3. 明确交付物路径（SQL、镜像 tag、包路径、验证 URL）
4. 若需用户操作，最多 1 项并说明原因

## 配置
向用户索要 key/env 之前：
1. 搜当前仓库（`.env*`、docker-compose、k8s）
2. 搜兄弟项目（anycode、digital-story-platform、818cloud、有糖等）
3. 整理打码配置表供确认，不让用户自己找

## 改完三件套
1. 重启/重载服务（不等用户问「重启了吗」）
2. 回归同线程已报 bug
3. 收尾格式：做了什么 → 如何验证 → 产物在哪 → 还需用户做什么（如有）

## 截图 / 「还是不对」
- 先对照截图理解预期 vs 实际再改
- UI 改动确认是宽/高/对齐/文案哪类问题
- 同一 bug 第二次出现 → 先写根因再修，不堆补丁

## 大计划
先交付一个可演示里程碑（起服务 → 登录 → 核心流程跑通），再扩展。核心链路为空时不得勾选全部 todo。

## 长任务默认
- 长篇生成：后台 job + DB 持久化 + 前端进度轮询/WebSocket
- LLM 流式：SSE，支持断线恢复
- 部署：回复中写镜像 tag、registry、SQL 迁移路径

## 安全红线
- 微信 DB：只读快照，禁止直接打开 live DB
- 支付：用测试套餐自测回调后再说完成
- 密钥：不进 git；镜像内嵌证书需注明风险
