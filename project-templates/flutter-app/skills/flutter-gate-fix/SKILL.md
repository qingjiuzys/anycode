---
name: flutter-gate-fix
description: Run flutter analyze/test gates and fix failures iteratively.
description_zh: 运行 flutter analyze/test 门禁并迭代修复。
category: qa
---

# flutter-gate-fix

Run `flutter analyze` and `flutter test` (or project `scripts/verify.sh` when present). Fix analyzer and test failures until gates pass.
